# BACKUP.md — 백업·복구 대장

> 작성 2026-09-09. **이 저장소를 clone 해도 되살아나지 않는 것**들의 목록과 복구 경로.
> 아직 조치는 하지 않았다 — 이 문서는 **무엇이 위험한지 합의하기 위한 것**이다.
>
> 배경: EC2 인스턴스가 사라졌을 때 무엇을 잃는지 따져 보다 작성. 결론부터 —
> **앱은 멀쩡히 돌지만(발행분은 R2·Supabase 에 있다) 새 산을 추가하는 능력을 잃는다.**

---

## 1. 한눈에

| 자산 | 크기 | 위치 | 백업 | 잃으면 |
|---|---|---|---|---|
| **산림청 등산로 원본** `mountain/` | **2.6 GB** (11,733파일) | EC2 only | ❌ **없음** | 🔴 산 신규 등록 불가 |
| DEM 캐시 `cache/dem` | 733 MB | EC2 only | ❌ 없음 | 🟡 등고선·고도 재계산 불가(재취득 가능) |
| `cache/kr-terrain.mbtiles` | 280 MB | EC2 only | ❌ 없음 | 🟡 지형 타일 재빌드 필요 |
| 마스터 벡터타일 `kr-base.pmtiles` | 482 MB | EC2 + **R2** | ✅ R2 | — |
| 지형 타일 `kr-terrain.pmtiles` | 274 MB | EC2 + **R2** | ✅ R2 | — |
| 산별 발행 팩 `data/packs/` | 14 MB | EC2 + **R2** | ✅ R2 | — (초안에서 재생성 가능) |
| 초안·원본 GPX `admin_data/` | 소 | git | ✅ git | — |
| 코드·문서 | 소 | git(52MB) | ✅ GitHub | — |
| **`.env`** (13줄) | 소 | EC2 only | ❌ 없음 | 🔴 R2·Supabase service·기상청 키 전부 |
| **Supabase 데이터** | 소 | Supabase | ❓ **미확인** | 🔴 사용자 등반 기록 |

---

## 2. 🔴 우선순위 1 — `mountain/` (산림청 등산로 원본)

```
2.6 GB · 11,733 파일 · 2,932개 산 · .gitignore 로 제외
산 하나당 SHP/DBF/PRJ/SHX/JSON/GPX 6종 × (등산로 + 스팟)
```

**이게 가장 위험하다.** 이유:

- **저장소에 없다** (`.gitignore: mountain/`). clone 으로 복구되지 않는다.
- **자동 취득 스크립트가 없다.** `scripts/` 에 `fetch_top100.py`(100대 명산 목록)·
  `fetch_peak_poi.py`(봉우리 POI) 는 있지만 **등산로 원본을 받는 스크립트는 없다.**
  (`scripts/dl_gpx.bin` 은 이름과 달리 다운로더가 아니라 4MB GPX **데이터 파일**이다.)
  수동으로 모은 자산으로 보이며, 그래서 재수집 비용을 가늠하기 어렵다.
- **관리자 산 검색이 이 디렉터리 스캔이다**(`admin_server._mnt_codes`). 사라지면 검색 결과 0건이 되고
  **새 산을 등록할 수 없다.**
- 2026-09-09 확인: `MNT_CODE.xlsx` 2,931개 = 로컬 2,932개로 **보유분이 전국 완질**이다.
  다시 모으려면 같은 수집 작업을 처음부터 반복해야 한다.

**대책 후보**
1. R2 에 tar.gz 로 업로드 (압축 시 1GB 내외 예상 — 10GB 한도에 여유 있음).
2. 또는 git LFS. 다만 파일 11,733개는 LFS 에도 부담이라 tar 묶음이 낫다.
3. 최소한 **오프사이트 사본 1벌**(외장 디스크·다른 클라우드)이라도 둔다.

---

## 3. 🔴 우선순위 2 — `.env`

키 이름만 적는다(값은 이 문서에 적지 않는다). 2026-09-09 실제 확인:

```
ADMIN_TOKEN · ADMIN_PASSWORD            관리자 콘솔 인증
R2_ACCESS_KEY_ID · R2_SECRET_KEY · R2_ENDPOINT · R2_BUCKET
SUPABASE_SECRET_KEY                     service_role — RLS 우회
KNPS_KEY                                기상청·에어코리아 (코드가 KMA_KEY 없으면 이걸 쓴다)
PEAK_POI_KEY                            봉우리POI·100대 명산 API
```

- git 에 없고(당연) **다른 사본도 없다.** 인스턴스가 날아가면 키를 전부 재발급해야 한다.
- ⚠️ **service_role 키는 RLS 를 우회한다.** 백업 위치는 암호화된 비밀 관리 도구여야 하고,
  `IOS.md` §11 규칙대로 **맥북에 두지 않는다**.
- 대책: 1Password/키체인 등 비밀 관리자에 보관. 저장소·R2·평문 파일 금지.

---

## 4. 🔴 우선순위 3 — Supabase (사용자 데이터)

```
mountains(카탈로그) · mountain_info · climb_records · saved_packs · profiles
```

**`climb_records` 는 사용자가 만든 데이터라 재생성이 불가능하다.** 잃으면 끝이다.

- ❓ **현재 백업 여부 미확인.** 요금제에 따라 자동 백업·PITR 제공 범위가 다르므로
  Supabase 대시보드에서 **실제 설정을 확인해야 한다**(이 문서는 추정하지 않는다).
- 대책 후보: 주기적 `pg_dump` 또는 REST 로 테이블 덤프 → R2 보관. 규모가 작아 비용은 무시할 수준.

---

## 5. 🟡 우선순위 4 — 재취득 가능한 캐시

| 자산 | 재취득 방법 | 비용 |
|---|---|---|
| `cache/dem` 733 MB | Copernicus GLO-30 공개 데이터 재다운로드 (`dem_cache.ensure`) | 시간만 |
| `cache/kr-terrain.mbtiles` 280 MB | DEM 에서 재빌드 | 시간(길다) |
| `data/packs/` 14 MB | 초안(git) + DEM 으로 재발행 | 짧음 |

**복구는 가능하지만 시간이 든다.** DEM 은 전국분이라 재다운로드가 오래 걸린다.
R2 에 올려 두면 10GB 한도의 7% → 약 14% 가 되므로 여유는 충분하다.

---

## 6. ✅ 이미 안전한 것

- **코드·문서·초안·원본 GPX** — GitHub `ksthink/hihi`. 2026-09-05 확인: 삭제한 산의 원본 GPX 도
  `git checkout <커밋> -- admin_data/<코드>` 로 되살릴 수 있다.
- **마스터 타일 2개**(756 MB) — R2 에 그대로 있다. 사실상 오프사이트 백업 역할을 겸한다.
- **발행된 팩·config·이미지** — R2. 앱이 이걸 읽으므로 EC2 와 무관하게 서비스는 유지된다.

---

## 7. R2 여유 (2026-09-09 실측)

```
버킷 'hihi' — 객체 30개 · 0.77 GB
  (루트)   765.8 MB   kr-base 482.3 · kr-terrain 273.9
  images    20.9 MB
  packs      6.0 MB   (산 2개)
  config     0.0 MB
```

- **저장 용량은 문제가 아니다.** 무료 10GB 중 7.7% 사용. 산별 팩이 평균 2MB 남짓이라
  100개 산을 발행해도 +0.2GB 수준. 위 백업(mountain tar + DEM)을 다 올려도 여유가 있다.
- ⚠️ **실제 한도는 Class B(읽기) 요청 수**다. `kr-base.pmtiles` 를 HTTP Range 로 조각 읽으므로
  타일 한 장 = 요청 1건. 무료 1,000만 건/월. 사용자가 늘면 용량보다 요청이 먼저 닿는다.
  완화: **오프라인 팩**(다운로드하면 그 산은 요청 0건) + Cloudflare CDN 캐싱 규칙.
  ⚠️ 현재 사용자 2명이라 실측 근거가 없다 — Cloudflare 대시보드(R2 → Metrics)에서 확인할 것.

---

## 8. 정리 — 하지 않은 일

이 문서는 현황 기록이며 **아직 아무 백업도 수행하지 않았다.** 착수 순서 제안:

1. `.env` → 비밀 관리자 (5분, 위험 대비 가장 싸다)
2. Supabase 백업 설정 **확인** → 없으면 덤프 자동화
3. `mountain/` tar.gz → R2
4. `cache/dem` → R2 (선택 — 재취득 가능하나 시간이 든다)
