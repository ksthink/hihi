# oh-my-claudecode 상태
.omc/

# 스크래치/불필요 바이너리 (같은 데이터의 GPX 내보내기, 파이프라인에 미사용)
scripts/dl_gpx.bin

# DEM 원본(대용량) — 필요 시 AWS elevation-tiles-prod 에서 재다운로드
scripts/*.hgt
scripts/*.hgt.gz

# python
__pycache__/
*.pyc

# Supabase 비밀 (service_role/secret 키 등) — 절대 커밋 금지
.env
*.env

# OS
.DS_Store

# 기저 타일 추출본 (대용량) — Supabase Storage 로 배포, 재생성: pmtiles extract
data/tiles/


# 산림청 전국 등산로 원본 (2,932산 SHP/GeoJSON/GPX, 2.9GB) — 커밋 금지, 팩 빌드 소스
mountain/
