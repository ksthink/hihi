# WORD.md — 하이하잇 용어 사전

이 프로젝트에 실제로 등장한 용어만 모았다. 각 항목은 **뜻**과, 선택의 여지가 있었던 것은
**왜 이걸 골랐나(결정 배경)** 를 함께 적는다. 나중에 다시 봤을 때 "왜 이렇게 해뒀지?" 에
답하는 것이 이 문서의 목적이다.

- 관련 문서: [README.md](README.md) 전체 구조 · [IOS.md](IOS.md) 네이티브 이식 · [CLOUDFLARE.md](CLOUDFLARE.md) 타일 호스팅

---

## 목차

1. [지도를 그리는 기술](#1-지도를-그리는-기술)
2. [지도 데이터와 좌표계](#2-지도-데이터와-좌표계)
3. [오프라인](#3-오프라인)
4. [iOS 네이티브](#4-ios-네이티브)
5. [백엔드와 인프라](#5-백엔드와-인프라)
6. [보안과 인증](#6-보안과-인증)
7. [빌드·배포·도구](#7-빌드배포도구)
8. [이 프로젝트만의 약속](#8-이-프로젝트만의-약속)

---

## 1. 지도를 그리는 기술

### 래스터 타일 / 벡터 타일
지도를 256px 정사각형 조각으로 잘라 배달하는 방식이 **타일**이다. 조각이 **그림 파일**이면
래스터, **좌표와 속성의 데이터**면 벡터다.

**왜 벡터인가** — 그림이면 색을 바꾸려면 전부 다시 만들어야 한다. 벡터는 같은 데이터에
스타일만 갈아끼우면 흑백/다크모드가 즉시 나온다. 또 회전·확대해도 라벨이 항상 똑바로 선다.
등산 앱은 **다크모드 절전**과 **나침반 회전**이 핵심이라 벡터 외의 선택지가 없었다.

### PMTiles
수십만 개 타일을 **단일 파일 하나**에 담고, 필요한 타일만 그 파일의 일부 구간만 읽어 꺼내
쓰는 포맷. 이 프로젝트의 `kr-base.pmtiles`(대한민국 전역, 505MB) 가 그것이다.

**왜 이걸 골랐나** — 타일 서버를 운영하지 않아도 된다. 파일 하나를 정적 스토리지에 올려두면
끝이다. 서버 비용·장애 지점이 사라지고, 같은 파일을 **폰 안에 그대로 복사하면 오프라인 지도가
된다**. 하나의 포맷이 온라인/오프라인 양쪽을 다 커버한다.

### HTTP Range 요청
"이 파일의 1,000~2,000번째 바이트만 주세요" 라고 요청하는 HTTP 기능. PMTiles 가 505MB
파일에서 보이는 타일만 뽑아내는 원리가 이것이다. 응답 코드가 200이 아니라 **206
(Partial Content)** 이면 정상 동작이다.

> 타일이 안 나올 때 `curl -I -H "Range: bytes=0-6"` 로 206이 오는지 보는 게 1차 진단이다.

### MapLibre GL JS / MapLibre Native
벡터 타일을 실제로 화면에 그려주는 렌더링 엔진. **GL JS** 는 웹(브라우저), **Native** 는
iOS/Android 용이다. 이 프로젝트는 웹에서 GL JS, iOS에서 Native 6.27 을 쓴다.

**왜 이걸 골랐나** — Mapbox가 유료·API키 필수로 전환했을 때 갈라져 나온 오픈소스 판이다.
키도 사용량 제한도 없다. 그리고 **웹과 iOS가 스타일 JSON을 그대로 공유**한다 — 웹에서
디자인한 결과가 네이티브에서 그대로 나온다. 이게 "웹에서 80% 만들고 iOS로 옮긴다"는
로드맵의 기술적 근거였다.

### 스타일 JSON
지도의 **디자인 명세서**. "고속도로는 3px 검정 선", "산 이름은 줌 12부터 표시" 같은 규칙의
목록. 데이터(PMTiles)와 완전히 분리돼 있다. 이 프로젝트는 `basemap-style.js` 의
`buildStyle()` 이 생성하고, iOS 용은 `ios/gen-style.mjs` 가 파일로 뽑아낸다.

### 소스 / 레이어
- **소스(source)** = 데이터가 어디서 오는가 (PMTiles 파일, GeoJSON 파일)
- **레이어(layer)** = 그 데이터를 어떻게 그리는가 (선/원/글자, 색, 두께)

하나의 소스를 여러 레이어가 재사용한다. 등반 경로를 **두 겹**(굵은 검정 `climb-route-casing`
위에 회색 점선 `climb-route`)으로 그려 "검정 테두리 + 회색 점선" 을 만든 것이 그 예다.

### 글리프 (SDF Glyph)
지도 라벨용 **글자 이미지 묶음**. 폰트 파일을 그대로 쓰지 못하고, 문자들을 미리 특수한
이미지(`.pbf`)로 구워둔 것을 쓴다. SDF(Signed Distance Field) 방식이라 아무리 확대해도
글자가 깨지지 않고, 회전해도 외곽선이 선명하다.

**왜 직접 호스팅하나** — 예전에 외부 공개 서버(Protomaps 데모 버킷)가 파일을 지워서 지도
라벨이 통째로 사라진 적이 있다. 그 이후 글리프를 리포 `fonts/` 안으로 가져왔고, iOS에서는
**앱 번들에 통째로 넣는다**(44MB, CJK 전 범위). 네트워크가 없어도 라벨이 나와야 하므로
오프라인 앱에는 사실상 필수였다.

### localIdeographFontFamily
웹 MapLibre 전용 옵션. 한글·한자는 글자 수가 많아 글리프로 구우면 용량이 폭발하니,
**브라우저가 가진 폰트로 즉석 렌더**하게 하는 기능.

**왜 iOS에서 문제가 됐나** — MapLibre **Native 에는 이 기능이 없다**. 그래서 iOS용으로는
한글까지 전부 SDF 글리프로 구워 번들에 넣어야 했다. 웹에서 잘 되던 게 네이티브에서 안 되는
전형적인 사례.

### 음영기복 (Hillshade) / Terrain-RGB
지형의 높낮이에 가상의 햇빛을 비춰 만든 **입체감 있는 음영**. 고도값을 RGB 색상에 인코딩한
특수 타일(Terrain-RGB)을 재료로 쓴다.

**왜 중요한가** — 색을 다 뺀 흑백 지도에서 "여기가 능선이고 저기가 계곡" 을 알려주는 거의
유일한 시각 단서다. 단, 오프라인 팩에는 넣지 않는다(용량 대비 효용).

---

## 2. 지도 데이터와 좌표계

### OSM (OpenStreetMap)
누구나 편집하는 세계 지도 위키. 이 프로젝트 기저 지도의 원본 데이터.

**의무 사항** — 라이선스가 **ODbL** 이라 ① 출처 표시 ② 가공한 데이터를 배포하면 같은
조건으로 공개(share-alike). 앱스토어 출시 시 **저작자 표시 화면이 반드시 필요**하다.

### Overpass API
OSM 에서 "북한산 반경 안의 등산로만" 같은 조건 검색으로 데이터를 뽑는 질의 서비스.
`scripts/osm_trails.py`, `osm_routes.py` 가 이걸 쓴다.

### GeoJSON
좌표와 속성을 담는 JSON 표준 형식. 이 프로젝트의 `data/*.geojson`(등산로·시설·등고선)이
전부 이 형식이다. 텍스트라 사람이 열어볼 수 있고 디버깅이 쉽다.

**PMTiles 와의 역할 분담** — 전국 기저 지도처럼 **크고 안 바뀌는 것**은 PMTiles,
산별 등산로처럼 **작고 자주 고치는 것**은 GeoJSON.

### DEM / SRTM / Copernicus
**DEM**(Digital Elevation Model) = 땅의 높이를 격자로 기록한 데이터. **SRTM**(NASA 우주왕복선
측량)과 **Copernicus**(EU) 는 그 무료 공개 데이터셋 이름이다.

**용도** — ① 등고선 생성(`make_contours*.py`) ② 등산로 각 지점의 고도 채우기
(`add_elevation*.py`) → 여기서 코스의 **누적 상승고도**와 고도 프로파일 그래프가 나온다.

### 등고선 (Contour)
같은 높이의 점을 이은 선. DEM 을 재료로 직접 생성한다.

### 좌표계와 EPSG 코드
지구의 곡면을 숫자로 옮기는 방식이 여럿이라, 각 방식에 **EPSG** 번호가 붙어 있다.
이 프로젝트에 등장하는 셋:

| 코드 | 이름 | 이 프로젝트에서 |
|---|---|---|
| **EPSG:4326** | WGS84 (위도·경도) | GPS·GeoJSON·지도 표준. 최종 저장 형식 |
| **EPSG:5186** | 중부원점 (TM) | 산림청 등산로 원본이 이 좌표계 → 변환 필요 |
| **EPSG:5179** | UTM-K | 국가지점번호 계산의 기반 |

**왜 변환이 필요한가** — 국내 공공데이터는 미터 단위 평면좌표(5186 등)로 오는데, 지도
엔진과 GPS 는 위경도(4326)만 이해한다. `scripts/convert_*.py` 가 이 다리를 놓는다.

### 국가지점번호 (NPN)
산악 지역 위치를 `다사 5394 6231` 처럼 표기하는 국내 표준. 소방·구조 신고 시 위경도보다
전달이 정확하다. `npn.js` 가 WGS84 → UTM-K 변환으로 계산한다.

**왜 직접 구현했나** — 외부 라이브러리·API에 의존하면 **오프라인에서 못 쓴다**. 조난 상황은
정확히 통신이 안 될 때 온다. 순수 수학이라 브라우저 API를 하나도 안 쓰고, 그래서 iOS로
1:1 이식이 가능하다.

### 맵매칭 (Map Matching)
실제 GPS 궤적(GPX)을 지도의 등산로 선에 **달라붙게** 보정하는 것. 관리자 콘솔에서 실측
GPX 를 올려 코스를 만들 때 쓴다(`scripts/gpx_match.py`).

---

## 3. 오프라인

### 로컬 우선 (Local-first)
"네트워크는 있으면 좋은 것, 없어도 핵심 기능은 돌아간다" 는 설계 원칙.

**왜 이 앱의 전제인가** — 산에서는 통신이 끊긴다. 그래서 등반 런타임(지도 렌더·경로 표시·
GPS 기록)은 **항상 로컬**이 담당하고, 서버는 로그인·기록 동기화·카탈로그만 맡는다.
서버가 죽어도 등산은 계속된다.

### 오프라인 팩 (Pack)
산 하나를 통째로 폰에 담는 데이터 묶음. `packs/<산코드>/` 아래에:
- `base.pmtiles` — 그 산 영역만 잘라낸 기저 지도
- `routes / spots / contours.geojson` — 등산로·시설·등고선

**설치 위치** — iOS `Application Support/packs/<산코드>/`.

### `pmtiles://file:///…`
다운로드한 로컬 PMTiles 파일을 지도 엔진에 물리는 주소 형식. **MapLibre Native 6.27에서
동작을 실기기·비행기모드로 확인**했다(2026-07-20).

**왜 스타일을 통째로 다시 읽나** — 오버레이(GeoJSON)는 소스의 URL을 바꿔 끼울 수 있는데,
기저 벡터타일 소스의 주소는 **한 번 정하면 못 바꾼다**. 그래서 온라인↔오프라인 전환은
스타일 JSON의 주소를 치환한 사본(`offline-<리소스>-<산코드>.json`)을 만들어
**스타일 자체를 갈아끼우는** 방식으로 처리한다.

### NDJSON (개행 구분 JSON)
JSON 객체를 **한 줄에 하나씩** 이어 쓰는 형식. 등반 중 GPS 좌표를 이 형식으로 파일 끝에
계속 덧붙인다(`climb-session.ndjson`).

**왜 이 형식인가** — 일반 JSON은 저장할 때마다 전체를 다시 써야 하지만, NDJSON은 **한 줄
덧붙이기(append)** 로 끝난다. 앱이 갑자기 죽어도 그때까지 쓴 줄은 남는다.
사용자가 앱 스위처로 강제 종료하면 **종료 알림을 받을 보장이 없고 자동 재실행도 안 되므로**,
"끝날 때 저장" 이 아니라 "가는 동안 계속 저장" 이 유일한 해법이었다. 마지막 줄이 잘려 있을
수 있어 파싱은 관대하게 한다.

---

## 4. iOS 네이티브

### SwiftUI
애플의 선언형 UI 프레임워크. "이런 조건이면 이런 화면" 을 기술하면 알아서 갱신된다.
이 앱의 화면은 전부 SwiftUI 이고, 지도만 MapLibre(UIKit)를 감싸 넣었다.

### 안전 영역 (Safe Area) / `ignoresSafeArea`
노치·홈 인디케이터 등에 가리지 않는 화면 영역. `ignoresSafeArea` 는 그 밖까지 그리겠다는
선언이다.

**빠졌던 함정** — 이 선언은 **자식이 아니라 컨테이너에** 걸어야 한다. 부모가 이미 안전 영역을
소비한 뒤라 자식에만 걸면 확장되지 않는다(등반 HUD 하단이 비던 문제의 원인).
정답 조합은 컨테이너에 `ignoresSafeArea` + 카드 안쪽에 `safeAreaPadding` — 기기별 인디케이터
높이가 자동 반영돼 SE부터 Pro Max까지 한 번에 대응된다.

### 디텐트 (Detent)
바텀시트가 멈추는 **높이 단계**. 처음엔 접힘/중간/펼침 3단이었으나, 중간 단계가 하는 일이
없어 **접힘(peek) / 펼침(large) 2단으로 단순화**했다.

### CoreLocation
iOS의 위치 프레임워크. 이 앱의 설정:
- `kCLLocationAccuracyBest` — 최고 정확도
- `activityType = .fitness` — "도보 이동" 임을 알려 보정 품질을 높임
- `pausesLocationUpdatesAutomatically = false` — **필수**. 안 끄면 iOS가 "멈춘 것 같다"고
  판단해 임의로 위치 갱신을 끊는다(휴식 중 궤적 유실)
- `horizontalAccuracy > 50m` 인 점은 버림 — 튀는 좌표 제거

### 백그라운드 모드 (`UIBackgroundModes: location`)
앱이 화면 밖에 있어도 위치를 계속 받게 하는 권한 선언.

**함정** — 코드에서 `allowsBackgroundLocationUpdates = true` 를 켜면서 이 선언을
`Info.plist` 에 안 넣으면 **앱이 크래시**한다. 항상 짝으로 다닌다.
권한은 `WhenInUse` + 백그라운드 모드면 충분하고 `Always` 는 불필요하다.

### ATS (App Transport Security)
"암호화 안 된 HTTP 통신은 막는다" 는 iOS 기본 정책.

**어떻게 해소했나** — 예외 설정(`NSAllowsArbitraryLoads`)으로 뚫는 대신, 통신 경로를 전부
HTTPS로 바꿔 **예외 자체를 제거**했다. 앱스토어 심사에서 예외는 소명 대상이 되므로, 없는
편이 낫다.

### Keychain vs UserDefaults
로그인 세션 같은 민감 정보의 저장소. Keychain은 암호화 금고, UserDefaults는 평범한 설정 저장소.

**왜 UserDefaults 인가** — **서명이 없는 시뮬레이터 빌드는 Keychain 쓰기가 실패**해서
탭만 바꿔도 로그아웃됐다. 개발 편의를 위해 UserDefaults 로 바꿨다.
⚠️ **서명된 배포 빌드에서는 Keychain 으로 되돌릴지 재검토할 것.**

### XcodeGen / `project.yml`
Xcode 프로젝트 파일(`.xcodeproj`)을 **YAML 텍스트로부터 생성**하는 도구.

**왜 쓰나** — `.xcodeproj` 는 사람이 못 읽는 XML 덩어리라 Git 충돌이 지옥이다. `project.yml`
한 장으로 관리하고 프로젝트 파일은 생성물로 취급한다.

### 폴더 참조 (`type: folder`)
Xcode에 파일을 넣는 두 방식 중 하나. 일반 그룹은 하위 폴더 구조를 무시하고 **번들 루트로
평탄화**하지만, 폴더 참조는 **디렉터리 구조를 그대로 유지**한다.

**왜 필요했나** — 글리프는 `MonaS12 Regular/0-255.pbf` 처럼 폰트별 폴더에 같은 파일명이
들어 있다. 평탄화되면 서로 덮어써서 라벨이 깨진다. 반드시 폴더 참조여야 한다.

---

## 5. 백엔드와 인프라

### Cloudflare R2
S3 호환 오브젝트 스토리지. 기저 타일(`kr-base.pmtiles`)과 오프라인 팩이 여기 있다.

**왜 R2 인가** — **egress(다운로드 트래픽) 요금이 0원**이다. 505MB 파일에 Range 요청이
쏟아지는 지도 앱에서 AWS S3 요금 구조는 감당하기 어렵다. 이 한 가지가 결정 요인이었다.

### 커스텀 도메인 vs 공개 개발 URL (`pub-*.r2.dev`)
R2 를 외부에 여는 두 가지 방법.

**왜 커스텀 도메인(`hihi.metaphr.dev`)으로 갔나** — `r2.dev` 개발 URL은 **요청량 제한이 걸리고,
일부 User-Agent를 403으로 막는다**(네이티브 앱의 기본 UA가 여기 걸렸다). 프로덕션에서 쓸
물건이 아니다.

> 🔎 **겪은 삽질** — 커스텀 도메인이 계속 엉뚱한 응답을 줬는데, 원인은 Cloudflare가 아니라
> **Vercel 쪽에 남아 있던 `*.metaphr.dev` 와일드카드 도메인**이었다. 인증서 발급자와
> 응답 헤더(`x-vercel-id` 유무)로 어느 쪽이 응답했는지 판별할 수 있다.

### CORS
"내 도메인 파일을 다른 도메인 웹페이지가 읽어도 되는가" 를 서버가 허락하는 규칙.

**웹만의 문제** — 브라우저에만 있는 제약이라, **iOS 네이티브에는 CORS 개념 자체가 없다.**
웹에서 프록시를 거쳐야 했던 경로가 네이티브에선 직결로 단순해지는 이유다.

### Vercel / 서버리스 함수
정적 사이트 호스팅 + 요청이 올 때만 잠깐 실행되는 작은 서버(`api/weather.js`, `api/tiles.js`).
상시 서버가 없어 관리 부담과 비용이 없다.

### 프록시 (Proxy)
클라이언트와 진짜 서버 사이에서 요청을 중계하는 것. 이 프로젝트엔 두 종류가 있고 **목적이
서로 다르다**:

| 프록시 | 목적 | 네이티브에서도 필요? |
|---|---|---|
| `/pmtiles/*` (`api/tiles.js`) | 브라우저 CORS 회피 | ❌ 불필요 (R2 직결) |
| `/api/weather` | **기상청 API 키 은닉** | ✅ **계속 필요** |

**핵심 구분** — CORS 프록시는 브라우저 제약을 우회하는 임시방편이라 네이티브에서 사라진다.
**키 은닉 프록시는 보안 장치라 영원히 남는다.**

### Supabase / Postgres
관계형 DB + 인증을 묶어 제공하는 서비스(서울 리전). 테이블은
`mountains`(산 카탈로그) · `mountain_info` · `profiles` · `climb_records` · `saved_packs`.

**역할 한정** — 파일 저장 기능도 있지만 **전부 R2로 옮겼다.** Supabase 는 인증과 DB만 맡는다.

### RLS (Row Level Security)
Postgres 기능으로, **"이 행은 이 사용자만 볼 수 있다"** 를 DB 자체가 강제한다.

**왜 결정적인가** — 앱 코드에 "내 것만 조회" 를 짜 넣는 방식은 코드를 우회하면 뚫린다.
RLS는 DB 레벨이라 우회가 불가능하다. 그래서 **공개 키를 앱에 그대로 넣어도 안전**하다 —
클라이언트 직결 구조의 전제가 이것이다.

> ⚠️ **조용한 실패 주의** — 세션과 RLS 정책이 어긋나면 서버는 **0행을 지우고 에러 없이 성공**을
> 반환한다. 삭제/수정은 `.select()` 로 영향받은 행을 돌려받아 0행인지 확인해야 한다.

### `.env`
API 키 같은 비밀값을 담는 파일. **Git에 커밋하지 않고, 앱에도 절대 포함하지 않는다.**
비밀값의 거처는 서버(Vercel 환경변수)뿐이다.

---

## 6. 보안과 인증

### 공개 키(publishable) vs 비밀 키(secret)
같은 서비스라도 키가 두 종류다.
- **공개 키** — 앱에 넣어도 되는 키. RLS 같은 보호 장치가 뒤를 받친다
- **비밀 키** — 모든 제약을 무시하는 마스터 키. **서버 전용**

**앱 바이너리는 비밀 저장소가 아니다.** 앱에 넣은 문자열은 추출이 가능하다고 전제해야 한다.
그래서 기상청 키(`KMA_KEY`)·R2 자격증명·Supabase secret 키는 앱에 넣지 않고 프록시 뒤에 둔다.
넣는 순간 도용과 쿼터 소진이 시간 문제가 된다.

### 이 프로젝트의 키 배치

| 키 | 어디에 | 이유 |
|---|---|---|
| Supabase 공개 키 | 앱·프런트엔드 | RLS 보호 |
| `KMA_KEY` (기상청) | Vercel 환경변수 | 앱에 넣으면 추출됨 → 프록시 경유 |
| R2 자격증명 | 로컬 `.env` | 업로드 전용, 앱은 읽기만 |
| `ADMIN_PASSWORD` | 로컬 `.env` | 관리자 콘솔은 로컬 전용 |

---

## 7. 빌드·배포·도구

### 정적 사이트 / 빌드 없음
webpack 같은 번들러 없이 브라우저가 파일을 그대로 실행하는 구조(ES 모듈 사용).

**왜** — 빌드 도구는 몇 년 지나면 그 자체가 부채가 된다. 이 규모에선 없는 편이 오래 간다.

### ESM (ES Modules)
`import` / `export` 로 파일을 나누는 자바스크립트 표준 모듈 문법. 브라우저가 직접 지원한다.

### `serve.py` / `admin_server.py`
로컬 개발용 파이썬 서버(포트 8890). 정적 파일 + `/pmtiles` 프록시.
`admin_server.py` 는 여기에 **관리자 콘솔**(산 등록·코스 편집·배포)이 더해진 버전.

> `python3 -m http.server` 로도 페이지는 뜨지만 프록시가 없어 **기저 지도가 안 나온다.**

### 산코드 (9자리)
산림청이 부여한 산 식별자. **DB 행·R2 폴더명·로컬 팩 키·앱 상태가 전부 이 코드로 연결**된다.
한 곳이라도 어긋나면 연결이 끊기므로, 이 프로젝트에서 가장 중요한 단일 식별자다.

### 데이터 파이프라인 (`scripts/*.py`)
원본 공공데이터 → 앱이 쓰는 형식으로 가공하는 스크립트 모음.

```
산림청·OSM 원본 ──convert_*/osm_*──> GeoJSON
                                      │
DEM(SRTM/Copernicus) ──add_elevation──┤ 고도 채움
                     ──make_contours──> 등고선
                                      │
                          publish_pack ──> R2 업로드 + Supabase 반영
```

### 실기 빌드 / 시뮬레이터

**시뮬레이터에서 검증 불가능한 것들** — Keychain(서명 없음), 실제 GPS, 배터리 소모,
네트워크 제약. 실제로 "맥에선 되는데 폰에선 안 되는" 문제를 여럿 겪었다.

> 🔎 **대표 사례** — 날씨만 안 나오고 지도는 나왔다. 원인은 날씨 프록시가 **비표준 포트
> 8890(HTTP)** 이라 셀룰러 망에서 아웃바운드가 막힌 것. 지도는 표준 443이라 멀쩡했다.
> **모바일에서 신뢰할 수 있는 것은 도메인 + HTTPS:443 뿐**이라는 교훈. Vercel(443)로
> 옮겨 해결했고, 덤으로 맥 의존이 0이 됐다.

---

## 8. 이 프로젝트만의 약속

### MonaS12
지도 라벨과 앱 UI에 **공통으로** 쓰는 폰트. 웨이트는 Regular / Bold 2종뿐이다.

- 지도 라벨 → SDF 글리프 PBF (`fonts/MonaS12 Regular/`)
- 웹 UI → `.woff2` 서브셋
- iOS UI → `.ttf` + `UIAppFonts`

> **코드 읽을 때 주의** — iOS 헬퍼 함수 이름이 `Font.kakao(size:weight:)` 인데, 이건 과거
> KakaoSmallSans 를 쓰던 시절의 잔재다. **실제로 반환하는 폰트는 MonaS12.** 호출부 89곳을
> 고치지 않으려 이름만 유지했다.
>
> 웨이트가 2종뿐이라 `semibold` 이상은 Bold, 나머지는 Regular로 근접 매핑한다. 그리고
> **커스텀 폰트는 `.fontWeight` 로 굵기가 안 바뀐다** — 반드시 `.font(.kakao(size:weight:))`
> 로 지정해야 한다.

### 흑백 디자인
색을 쓰지 않는다. **① 다크모드 절전**(OLED는 검정 픽셀이 전력을 안 쓴다 — 산에서 배터리는
안전 문제다) **② 정보 위계**(색이 없으면 등산로·현재 위치 같은 핵심에 시선이 간다)
**③ 가독성**(직사광선 아래에서 대비가 최선이다).

### 탭 구조
**탐험**(지도·코스 선택) · **추천** · **등반**(카드·시작·저장된 지도) · **기록**(캘린더·통계·프로필)

**등반 중에는 하단 네비바를 숨긴다** — 오터치 방지 + 화면 확보. 탭을 잠그고 아이콘을
남기는 방식도 시도했으나, 그냥 없애는 쪽이 명확했다.

### 등반 종료 확인 코드
종료 버튼 → **무작위 2자리 숫자** + 커스텀 키패드. 그 숫자를 입력해야 종료된다.
주머니 속 오터치로 몇 시간짜리 기록이 날아가는 걸 막는 장치다.

### 계획 경로 vs 지나온 경로
- **앞으로 갈 길** — 회색 점선 + 검정 테두리 (2겹 레이어)
- **지나온 길** — 검정 실선

### 현재 위치 버튼 (나침반 통합)
- **1번 누름** (꺼짐 또는 지도를 직접 움직인 뒤) — 현위치 중심 + **정북 고정** 추적, 기울기 복원
- **2번 누름** — **나침반 추적**. 기기가 보는 방향으로 지도가 회전한다
- **그 이후** — 정북 ⇄ 나침반을 계속 오간다

버튼 하나에 "현위치로" 와 "나침반" 을 합쳐 지도 위 버튼 수를 줄였다.

**왜 마커를 레이어로 바꿨나** — 기본 사용자 위치 표시(어노테이션)는 지도 갱신 주기가 달라서,
확대/축소할 때 **점과 경로가 어긋났다가 뒤늦게 따라붙었다**. 현재 위치를 일반 어노테이션이
아니라 **스타일 레이어(`climb-pos`)** 로 그리니 경로와 완전히 같은 타이밍에 움직인다.

### 스와이프 삭제 (`SwipeToDeleteRow.swift`)
기록·저장된 지도 목록 공통. 카드를 밀면 빨간 삭제 버튼이 드러난다.

**왜 직접 만들었나** — iOS 기본 `swipeActions` 는 행 **옆에 나란히** 놓이는 구조라
① 왼쪽 모서리 각짐 ② 카드와의 틈 ③ 카드가 버튼 위로 겹치는 효과 — 셋을 동시에 못 푼다.
ZStack 2층으로 직접 만들었고, 세로 스크롤과의 충돌은 **가로 우세 드래그**(|dx| > |dy|)로 피했다.

> 부수 함정: 전역 `.tint(검정)` 이 `role: .destructive` 의 빨강을 덮으므로 버튼에 `.tint(.red)`
> 를 명시해야 한다. 그리고 행 여백은 `listRowInsets`(안쪽)가 아니라 `listRowSpacing`(바깥)으로
> 줘야 버튼이 카드에 정확히 맞붙는다.

### 스파이크 (Spike) — S1 · S2 · S3
본 개발 전에 **"이게 되긴 하나"만 확인하고 버리는 검증 코드**. [IOS.md](IOS.md) §9의 go/no-go 관문.

| | 검증 대상 | 상태 |
|---|---|---|
| **S1** | 앱 번들 글리프로 한글 라벨 렌더 | ✅ 해소 (2026-07-20, 실기기) |
| **S2** | 오프라인 팩 다운로드 + 비행기모드 렌더 | ✅ 해소 (2026-07-20, 실기기) |
| **S3** | 백그라운드 GPS 배터리 (4시간 ≤25%) | ⏳ **미검증 — 실제 산행 필요** |

---

## 부록: 반복해서 배운 것

1. **"맥에선 되는데 폰에선 안 된다" = 네트워크 경로를 의심하라.** 비표준 포트, HTTP,
   IP 직접 접속은 모바일 망에서 막힌다. 도메인 + HTTPS:443 만 신뢰할 것.
2. **웹에서 되던 게 네이티브에서 안 되는 지점은 정해져 있다.** `localIdeographFontFamily`,
   CORS 프록시 전제, 브라우저 전용 API. 웹 코드를 쓸 때 이 경계를 미리 표시해 둘 것.
3. **UI 상수로 실측값을 대체할 땐 진행률을 반영하라.** GPS 고도가 부족할 때 코스 *계획*
   고도를 통째로 기록한 탓에, 0km 테스트 세션마다 315m가 쌓여 누적고도가 **2404m**로
   부풀었다. 계획값 대체는 **진행률 비례 + 최소 거리 게이트** 로.
4. **조용한 실패를 만들지 마라.** RLS 0행 삭제처럼, 에러 없이 성공을 반환하는 실패가 가장
   찾기 어렵다. 영향받은 행 수를 항상 확인할 것.
5. **강제 종료는 막을 수 없다.** 방어가 아니라 **가는 동안 계속 저장**이 답이다.
