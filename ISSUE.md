# ISSUE.md — 장기 미해결 이슈의 추적 기록

해결까지 오래 걸렸거나 여러 번 오진한 이슈를 **서술형으로** 남긴다.
"무엇을 고쳤나"보다 **"왜 그렇게 오래 걸렸나"** 에 무게를 둔다. 같은 함정을 다시 밟지 않기 위한 문서다.

---

## 1. 얼룩 폴리곤 — 지도에 나타나던 쐐기 모양 회색 덩어리

| | |
|---|---|
| **최초 보고** | 2026-07-10 |
| **해결** | 2026-07-22 (12일) |
| **원인** | `water` fill 레이어가 하천 **중심선(LineString)** 까지 채우고 있었다 |
| **수정** | `basemap-style.js` — water 레이어에 `geometry-type` 필터 추가 (`8629f29`) |
| **오진 횟수** | 6회 |

### 증상

지도를 보다 보면 지형과 무관한 회색 덩어리가 나타났다. 특징이 몇 가지 있었다.

- **가장자리가 완벽한 직선**이고 삼각형·쐐기 모양이다. 자연 지형이나 행정구역 경계로는 설명되지 않는다.
- **축척을 바꿀 때마다 모양이 달라진다.** 어떤 줌에서는 크게, 어떤 줌에서는 여러 조각으로 흩어진다.
- 웹앱과 iOS 앱 **양쪽에서 똑같이** 나타난다.
- 도심·평야·산지를 가리지 않고, 특히 하천 주변에서 자주 보인다.

### 왜 12일이 걸렸나 — 오진의 연쇄

**첫 번째 오진 (07-10) — OSM 숲 채움.**
처음엔 `landuse=wood/forest` 채움이 범인으로 보였다. 실제로 한국 OSM 의 숲 경계는 조악하고 줌별 일반화로 형태가 널뛴다. 숲 채움을 폐기하고 국립공원 채움을 `minzoom: 13` 으로 한정했다(`46fae42`). 증상은 줄었지만 사라지지 않았다.

이때 남긴 WORKLOG 의 "미해결 리포트"에 다음 후보로 **농지 채움(`landuse-farm`)** 과 **음영기복** 을 적어뒀다. 결과적으로 둘 다 아니었지만, 이 메모가 이후 조사의 출발점이 되면서 **잘못된 방향을 고착**시켰다.

**두 번째 오진 (07-21) — 음영기복의 평지/산지 밝기 차.**
벡터 채움을 아무리 뒤져도 화면을 덮을 만한 폴리곤이 없었다. PMTiles 에서 MVT 를 직접 디코딩해 확인한 결과:

- `landcover` 소스레이어는 해당 지역 타일에 **아예 없었다** (그리는 레이어가 무효였다)
- 우리가 칠하는 `landuse` 종류는 전부 타일 면적의 2% 미만이었다
- 정작 큰 폴리곤은 `wood` 27.8%, `cemetery` 14.7%, `forest` 9.7% 인데 **모두 칠하지 않는 것**들이었다

여기서 소거법으로 음영기복에 도달했다. 그럴듯한 메커니즘도 찾았다 — Copernicus DEM 은 수면을 상수 고도로 평탄화한다(서울 z12 타일 실측: 고도가 정확히 3.0m 인 픽셀 8.4%, 사방이 모두 같은 값인 픽셀 10.6%). MapLibre 음영 셰이더는 음영량에 `sin(경사)` 를 곱하므로 **경사 0 인 평지에는 아무것도 그리지 않는다.** 반면 비탈에는 하이라이트가 덧칠돼 밝아진다. 그래서 하천 유역이 상대적으로 어두운 띠로 남는다 — 그럴듯했다.

대비를 완화했다(`f8fe709`, `#ffffff→#fbfbfb`, 강조 0.35→0.24). **효과 없었다.**

**세 번째 오진 (07-22) — 하이라이트를 밑바탕색과 정렬.**
"낙차만 줄였을 뿐 구조가 그대로였다"고 판단해, 하이라이트를 밑바탕색과 **정확히 같게** 맞췄다(`baf4cfa`). 이러면 평지와 밝은 비탈이 같은 색이 되어 띠가 생길 수 없다는 논리였다. **또 효과 없었다.**

**네 번째 — 음영기복 완전 제거 (`a342e85`).**
사용자 요청으로 아예 껐다. 이때 이미 반증이 하나 있었다 — 제보된 스크린샷이 300m 축척(z≈15.5)이었는데, 그 구간의 `exaggeration` 은 z15 에서 0.12 → z16 에서 0 으로 **이미 거의 꺼져 있었다.** 그런데도 얼룩은 더 뚜렷했다. 이 모순을 짚어 기록해뒀지만, 원인을 찾는 데까지는 가지 못했다.

**껐는데도 얼룩은 그대로였다.** 여기서 음영기복 가설이 완전히 죽었다.

### 왜 데이터 검사가 계속 "정상"이라고 답했나

이 시점까지 타일 데이터를 여러 각도로 검사했고 **전부 통과**했다.

```
과대 폴리곤        없음 (칠하는 종류 전부 2% 미만)
꼭짓점 수          최대 934 (삼각분할 한계 65536의 1.4%)
좌표 범위          -128 ~ 4224 (정상 버퍼 안, 이탈점 0)
자기교차           0개
구멍 위치·감김방향  위반 0개
레이어별 독립 렌더  water·landuse-farm·landuse-park·earth 전부 정상
```

MVT 파서를 직접 짜서 Pillow 로 렌더까지 해봤는데 깨끗한 강과 정상적인 필지만 나왔다. **여기가 함정이었다.**

파서에 이 한 줄이 있었다.

```python
if gtype != 3 or not geom: continue          # 폴리곤만
```

처음부터 **폴리곤(`gt=3`)만 파싱하고 선(`gt=2`)은 건너뛰고 있었다.** 채움 레이어를 조사하는 중이니 폴리곤만 보면 된다고 여겼는데, 바로 그 가정이 틀렸다. 그래서 "데이터는 정상"이라는 결론을 여섯 번 반복해 냈고, 그 잘못된 전제 위에 가설을 계속 쌓았다.

### 결정적 전환 — 레이어를 하나씩 꺼보기

추측이 바닥나자 웹앱에 디버그 훅을 넣었다(`5082a2a`).

```
?hide=landuse-park       한 레이어만 숨김
?hide=*fill              채움 전부 숨김
```

사용자가 실기기에서 확인한 결과는 두 줄이었다.

- `?hide=*fill` → **얼룩 사라짐** (채움 레이어 중 하나다)
- `?hide=water` → **얼룩 사라짐** (물이다)

12일간의 소거법보다 이 두 줄이 결정적이었다.

### 진짜 원인

`water` 소스레이어를 기하 타입별로 세어보니 이랬다.

```
water 피처 구성 (z12~14 실측)   면 32 · 선 52 · 점 4
```

**하천 중심선이 면보다 많았다.** 그런데 스타일은 이렇게 돼 있었다.

```js
{ id: "water", type: "fill", source: "protomaps", "source-layer": "water",
  paint: { "fill-color": C.water } }
```

기하 타입을 가리는 필터가 없다. `fill` 레이어가 **열린 선을 삼각분할해 채운다.** 그 선들만 골라 채움으로 렌더해보니 제보된 얼룩과 **똑같은 쐐기·삼각형**이 나왔다.

모든 관찰이 여기서 설명된다.

| 관찰 | 설명 |
|---|---|
| 축척마다 모양이 변함 | 줌별로 선의 단순화 결과가 달라짐 |
| 가장자리가 완벽한 직선 | 삼각분할 산출물 |
| 웹·네이티브 공통 | 둘 다 같은 스타일·같은 tessellation |
| 하천 주변에 많음 | 하천 중심선이니 당연 |
| 데이터 검사는 전부 통과 | 면만 검사했으니 통과할 수밖에 |

### 수정

```js
filter: ["match", ["geometry-type"], ["Polygon", "MultiPolygon"], true, false]
```

하천 선 표현은 `rivers` 레이어(`physical_line`)가 이미 담당하므로 **잃는 표현이 없다.**
채움으로 그리는 다른 소스레이어(`earth`·`landuse`·`landcover`·`buildings`)는 전부 면만 있어 같은 위험이 없음을 확인했다.

이후 음영기복을 07-20 이전 원본 값으로 복구했다(`7f7b172`). 오진 과정에서 넣은 두 차례 완화(대비 축소·하이라이트 정렬)는 얼룩에 효과가 없었고 지형 입체감만 깎았으므로 함께 되돌렸다.

### 교훈

1. **소스레이어에 여러 기하 타입이 섞여 있을 수 있다.** 벡터 타일에서 `fill` 레이어를 쓸 때는 `geometry-type` 필터를 기본으로 넣는 편이 안전하다. 이름이 `water` 라고 해서 면만 있을 것이라 가정하면 안 된다.
2. **검사 도구의 전제가 곧 사각지대가 된다.** "채움을 조사하니 폴리곤만 보면 된다"는 합리적인 가정이 12일을 잡아먹었다. 검사가 계속 "정상"이라고 답하면 **검사 대상 자체를 의심**해야 한다.
3. **소거법은 후보 목록이 완전할 때만 작동한다.** 목록에 없는 것은 영원히 소거되지 않는다. 07-10 의 후보 메모가 이후 조사를 그 목록 안에 가둬버렸다.
4. **실제 렌더에서 한 겹씩 끄는 게 정적 분석보다 빠르다.** `?hide=` 훅은 30분이면 만들 수 있었는데 12일째에 만들었다. 재현 가능한 시각 버그는 **일찍 도구를 만드는 편이 싸다.**
5. **모순되는 증거를 만나면 거기서 멈춰야 한다.** "300m 축척에서는 음영이 이미 꺼져 있는데 얼룩은 더 뚜렷하다"는 사실은 3일 전에 이미 알고 있었고 기록까지 했다. 그런데도 같은 방향으로 두 번 더 시도했다.

### 관련 커밋

| 커밋 | 내용 |
|---|---|
| `88970a1` | 음영기복 도입 (얼룩의 발단이 된 시기) |
| `46fae42` | 1차 시도 — OSM 숲 채움 폐기·공원 z13+ |
| `f8fe709` | 2차 시도 — 음영 대비 완화 (오진) |
| `baf4cfa` | 3차 시도 — 하이라이트를 밑바탕색과 정렬 (오진) |
| `a342e85` | 4차 — 음영기복 완전 제거 (오진 확인) |
| `5082a2a` | **디버그 훅 `?hide=` 추가 — 전환점** |
| `8629f29` | **원인 규명·수정 — water 에 geometry-type 필터** |
| `7f7b172` | 음영기복 복구 (원본 값) |

---

## 2. 선택한 코스가 항상 1번으로 시작되던 문제

| | |
|---|---|
| **보고** | 2026-07-22 |
| **해결** | 2026-07-22 (같은 날, 4회 배포) |
| **원인** | `.task(id:)` 가 뷰 재등장마다 재실행되며 선택을 덮어씀 |
| **수정** | `ExploreView.swift` (`8ce0fbe`) |

### 증상

북한산에서 5번 코스(아인쌤 야호~)를 골라 등반을 시작하면, 헤더와 HUD 가 1번 코스(원효봉)를 가리켰다. 추천 탭의 큐레이션 카드로 진입해도 마찬가지였다.

### 원인이 셋 겹쳐 있었다

**① 큐레이션이 코스명을 버렸다 (`1d3136c`).**
추천 카드가 `onOpen(it.code)` 로 **산코드만** 넘기고 코스명을 넘기지 않았다. 산만 맞고 코스는 `courses.first` 로 떨어졌다.

**② 낡은 오프라인 팩 (`5f733d8`).**
코스 목록·헤더·시종점 마커는 `PackLoader` 가 **원격 R2** 에서 읽는데, 지도의 코스 선과 번호 배지는 `applyOverlay` 가 **로컬 팩** 에서 읽고 있었다. 팩 버전 관리가 없어 재다운로드 계기가 없으므로, 한번 받은 뒤 추가된 코스는 목록에는 뜨지만 지도에는 영원히 안 나온다. "코스는 선택됐는데 선이 안 그려진다"의 정체.

**③ 탭 전환마다 초기화 — 진짜 주범 (`8ce0fbe`).**
`.task(id: catalog.selected?.id)` 를 "id 가 바뀔 때만 실행된다"고 이해했는데 틀렸다. **`.task` 는 뷰가 사라질 때 취소되고 다시 나타날 때 재시작된다.** TabView 에서 탭을 오갈 때마다 실행된다는 뜻이다. 코스 자동선택이 무조건 `?? courses.first` 로 덮어쓰고 있었으므로:

```
탐험에서 5번 선택 → 등반 탭 → [등반 시작] → onStart 가 tab=0 →
탐험 재등장 → task 재실행 → climb.course = courses.first → 1번
```

큐레이션과 무관하게 **탭을 오가기만 해도** 초기화되고 있었다.

### 수정

우선순위를 두고, 등반 중에는 아예 손대지 않는다.

```
큐레이션 지정 > 이미 고른 코스 유지 > 복구된 세션 > 첫 코스
```

등반 중 `climb.course` 가 바뀌면 HUD 뿐 아니라 **저장되는 기록의 코스명·계획고도까지 어긋난다.** 이는 앞서 겪은 누적고도 부풀림(2026-07-20)과 같은 계열의 위험이다.

### 교훈

- **`.task(id:)` 는 "id 가 바뀔 때만"이 아니다.** 초기화 성격의 코드를 여기 두면 사용자의 선택을 매번 덮어쓴다. TabView 안에서는 특히 그렇다.
- **같은 데이터를 두 경로로 읽지 마라.** 목록은 원격, 지도는 로컬이면 "목록엔 있는데 지도엔 없는" 유령 증상이 난다.
- **빌드 성공은 검증이 아니다.** 세 번 연속 `BUILD SUCCEEDED` 만 확인하고 실제 진입 경로를 따라가지 않아 같은 증상으로 네 번 배포했다.

---

## 3. 네이티브 앱에 전철역·지명·POI 라벨이 통째로 안 나오던 문제

| | |
|---|---|
| **증상 지속** | 오래(웹엔 나오는데 앱엔 안 나옴) — 2026-07-25 규명·수정 |
| **원인** | 스타일의 **글리프 URL 이 상대경로**라 MapLibre Native 가 번들 file:// 로 resolve 못 함 |
| **수정** | `MapView.runtimeStyle` 이 런타임에 glyphs 를 번들 절대 file:// 로 재작성 (`a394f62`) |
| **오진 횟수** | 4회 (표시설정 / 대용량 pmtiles 전달 / 줌·모드 / 심볼 레이어) |

### 증상

관리자 지도(웹)에는 전철역·주차장·지명·아파트단지가 정확히 나오는데, **네이티브 앱 지도에는 하나도 안 나왔다.** 도로·철도선(라인 레이어)만 보이고 **어떤 텍스트 라벨도 없었다.** 사용자는 "전철역이 안 나온다"로 보고했다.

### 왜 오래 걸렸나 — 오진 4연쇄

증상이 "특정 종류(POI)가 안 나온다"로 읽혀, 데이터·설정·전달 문제를 차례로 의심했다. 실제로는 **텍스트 렌더 자체가 죽어 있었는데**, 지명 라벨이 어쩌다 보이던 화면(계양 지형모드)만 보고 "라벨은 되는데 POI 만 안 된다"고 오해했다.

1. **표시 설정 미연동으로 오진** → `gen-style.mjs` 가 관리자 config 를 빌드 시 반영하도록 고침(빌드 213). 스팟에는 유효했지만 전철역은 그대로.
2. **대용량 pmtiles 전달 문제로 오진** → kr-base(159,276타일·리프 디렉터리)를 네이티브가 못 읽는다고 판단. `{z}/{x}/{y}` 엔드포인트를 만들어 로컬 검증하려 했으나 **시뮬레이터가 Mac 의 로컬 http 엔드포인트에 닿지 못해** 검증 자체가 무산.
3. **줌·모드 문제로 오진** → 도시 모드 + z14/z15 강제. 여전히 안 나옴.
4. **심볼 레이어(pois) 자체 문제로 오진** → 작은 추출 pmtiles 로 테스트했는데, **그 추출본이 라벨을 못 싣는 것**이어서 A/B 판별까지 오염됐다.

### 결정적 전환 — os_log

시뮬레이터 하네스가 계속 오도해, **실기기에서 "안나와" 확인 → os_log 캡처**로 방향을 틀었다. 로그에 답이 있었다:

```
Error Code=-1002 "unsupported URL"
NSErrorFailingURLStringKey=glyphs/MonaS12%20Regular/0-255.pbf
```

기본 글자 범위(0-255)의 글리프조차 못 불러오고 있었다 → 글리프가 없으니 **모든 텍스트 라벨이 통째로 안 그려진 것**. "POI 만 안 나온다"가 아니라 "라벨 전체가 죽었다"였다.

### 진짜 원인

스타일의 `glyphs` 가 상대 템플릿 `"glyphs/{fontstack}/{range}.pbf"` 인데, MapLibre Native 가 이를 **번들 스타일의 file:// 기준으로 resolve 하지 못하고** 상대 문자열 그대로 CFNetwork 에 넘겼다. CFNetwork 는 scheme 없는 URL 을 "unsupported URL"(-1002)로 거부 → 글리프 로드 전부 실패. **웹(MapLibre GL JS)은 같은 상대 URL 을 정상 resolve** 하므로 웹엔 나오고 네이티브만 안 나왔다.

### 수정

- `MapView.runtimeStyle`: 스타일을 런타임에 재작성해 `glyphs` 를 **번들 절대 file://** (`Bundle.main.bundleURL/glyphs/{fontstack}/{range}.pbf`)로. 오프라인 base 소스 교체도 여기로 통합(구 `offlineStyleURL` 대체·삭제).
- `gen-style.mjs`: 글리프 폴더를 **공백 없는 이름**(`MonaS12Regular`/`MonaS12Bold`)으로 번들 + 스타일 `text-font` 도 공백 제거 — file:// URL 의 공백(%20) 문제 방어.
- 웹은 무변경.

### 교훈

- **"일부만 안 나온다"가 실은 "전부 죽었다"일 수 있다.** 어쩌다 보이던 라벨 한둘(지형모드 지명)에 속아 "라벨은 되는데 POI 만" 이라고 3번 오진했다. 안 나오는 것뿐 아니라 **나오는 것의 범위**도 정확히 확인했어야 했다.
- **시뮬레이터를 못 믿을 땐 실기기 + os_log.** 로컬 엔드포인트 도달 실패·추출 pmtiles 결함으로 시뮬레이터 검증이 세 번 오도했다. os_log 한 줄이 몇 시간의 추측을 끝냈다.
- **웹에서 되는 게 네이티브에서 되는 걸 보장하지 않는다** — 특히 상대 URL resolve 처럼 플랫폼 HTTP 스택에 기대는 부분.
- **글리프 URL 은 네이티브에선 절대경로로.** 상대 글리프 템플릿 + 번들 스타일 조합은 MapLibre Native 에서 조용히 실패한다.

### 관련 커밋

| 커밋 | 내용 |
|---|---|
| `a394f62` | ★ 글리프를 번들 절대 file:// 로 재작성 — 라벨·전철역 렌더 복구 |
| `e1a1724` | (선행) gen-style 이 빌드 시 R2 표시 config 반영 — 스팟은 이걸로 해결 |

---

## 4. 내비 화살표가 진행 방향의 역방향을 가리킴

| | |
|---|---|
| **최초 보고** | 2026-08-01 (실기기 — 코스에서 15m 벗어나 정방향 보행 중) |
| **해결** | 2026-08-02 |
| **원인** | `CourseNav.isForward` 가 이동 방향이 아니라 **"시작점·도착점 중 어디에 가까운가"** 로 등반/하산을 판정 + NavView 진입 시 **1회만** 판정 |
| **수정** | `CourseNav.NavDirector` 신설 — 움직임 기반 판정 + 지속·히스테리시스 + 수동 토글 |

### 증상

내비 화면(지도 없는 방향 안내)에서 코스 정방향으로 걷는데 화살표가 걸어온 방향(뒤)을
가리켰다. 보고 당시 코스에서 15m 정도 벗어나 있었지만 **이탈은 무관하다** — 이탈 15m 는
30m 경고 임계 아래고, look-ahead 목표 계산은 이탈량에 좌우되지 않는다.

### 원인 — 위치 기반 1회 판정

```swift
// CourseNav.swift:115 — 시작점에 더 가까우면 정방향
return ds <= de
```

**코스 중간점을 지난 뒤 내비 화면을 열면** 도착점이 더 가까우므로 하산(역방향)으로
오판한다 → 좌표열을 뒤집어 "코스를 따라 40m 앞"을 계산 → 목표가 시작점 쪽 = 걸어온 방향
→ 화살표가 정확히 뒤를 가리킨다. 판정이 진입 시 1회뿐이라 아무리 걸어도 안 바로잡힌다.
(이때 헤더 라벨도 "하산"으로 떠 있다 — 재현 시 확인 포인트.)

대림도-신도림(1.2km) 기준 약 600m 지점 이후 진입이 재현 조건. 이 휴리스틱은 "정상에서
하산 시작" 상황을 위한 것이었으나 **중간 진입** 사례를 놓쳤다.

### 수정 — 제안 3안을 모두 반영 (`NavDirector`)

내비는 지도를 보지 않는 화면이라 방향 오판이 곧 안전 문제다. 그래서 한 가지 판정에 기대지
않고 세 겹으로 쌓았다.

**① 움직임 기반.** 위치가 아니라 **"정방향 기준 남은 거리가 줄어드는가"** 로 본다.
이동 벡터와 코스 방향의 내적 대신 이 척도를 고른 이유는, 굽잇길에서 순간 방위가 뒤집혀도
코스를 따른 누적값은 흔들리지 않기 때문이다. 코스에서 벗어나 걸어도 부호는 유지된다.
진입 시에는 **진행 중 트랙**(`ClimbStore.track`)에서 현재 위치와 30m 이상 떨어진 과거 점을
찾아 비교하므로, 중간 진입이어도 첫 화면부터 방향이 맞는다.

**② 지속 판정 + 히스테리시스.** 반대 방향이 25초 이상, 그동안 실제로 25m 이상 움직였을 때만
뒤집는다. 정지 중(3m 미만)에는 아무 판정도 하지 않는다 — GPS 지터로 화살표가 펄럭이면
"틀린 방향을 자신 있게 가리키는" 것과 같은 위험이다.

**③ 수동 안전판.** 헤더의 "등반/하산" 라벨이 탭 가능한 캡슐이 됐다. 누르면 즉시 뒤집히고
이후 자동 전환이 멈춘다(자물쇠 아이콘 표시). 판정이 틀려도 사용자가 교정할 수 있다.

### 단위 검증에서 걸린 두 번째 결함

`CourseNav.swift` 는 Foundation·CoreLocation 만 의존하므로 `swiftc` 로 단독 컴파일해
8개 시나리오를 돌렸다. 여기서 **처음 구현이 실제로는 영영 방향을 바꾸지 못한다**는 걸 잡았다.

기준점(`lastCoord`)을 위치 갱신마다 옮기고 있었는데, 1Hz 보행은 한 걸음이 1.2~1.4m 라
3m 게이트를 **매번** 통과하지 못한다. 즉 "3m 이상 움직이면 판정"이 아니라 "한 번도 판정 안 함"
이었다. 기준점을 **게이트를 넘을 때만** 옮기도록 고쳐, 2~3초마다 한 번씩 판정하게 했다.

| 시나리오 | 결과 |
|---|---|
| 중간점 이후 + 정방향 트랙 (재현 조건) | 등반 ✓ (기존 `isForward` 는 하산으로 오판) |
| 중간점 이후 + 역방향 트랙 | 하산 ✓ |
| 짧은 역주행 10초 | 유지 ✓ |
| 지속 역주행 40초 | 반전 ✓ |
| 정지 60초 | 유지 ✓ |
| 수동 토글 후 정방향 120초 | 잠금 유지 ✓ |

### 교훈

- **"위치"로 "방향"을 추정하지 마라.** 시작점·도착점 근접 비교는 코스 전반부에서만 맞는
  휴리스틱이었는데, 그 전제가 코드 어디에도 적히지 않아 중간 진입 사례를 아무도 검토하지
  않았다. 방향은 움직임에서 나와야 한다.
- **게이트는 "얼마나 자주 통과하는가"까지 계산해야 한다.** 3m 게이트 자체는 합리적이었지만
  기준점 갱신 주기와 결합하자 통과 확률이 0 이 됐다. 빌드가 통과하고 코드도 그럴듯해
  실기기에서 "왜 안 바뀌지"로 한참 헤맬 뻔했다.
- **순수 계산 로직은 앱 없이 검증할 수 있다.** UIKit 의존이 없는 파일은 `swiftc` 로 떼어
  돌리면 실기기 산행 없이 시나리오를 검증할 수 있다 — 이 이슈에서 가장 값싼 도구였다.

### 관련 파일

| 파일 | 내용 |
|---|---|
| `ios/HiHeight/CourseNav.swift` | ★ `NavDirector` 신설 — 움직임 판정·히스테리시스·수동 잠금 |
| `ios/HiHeight/NavView.swift` | 진입 시 트랙 기반 초기화, 위치 갱신마다 `update`, 헤더 탭 토글 |

---

## 남아 있는 관련 과제

- **팩 버전 추적** — 오프라인 상태에서는 여전히 낡은 팩을 본다. 카탈로그 `pack_version` 비교 → 갱신 알림 또는 자동 재다운로드가 근본 해결. `Mountain` 모델이 `pack_version` 을 디코드하지 않고 `AuthStore` 는 `pack_version: 1` 하드코딩 상태다.
- **배터리 예산 실측** — 백그라운드 GPS 4시간 ≤25% (IOS.md S3). 실제 산행 1회로만 확인 가능.
