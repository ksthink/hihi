import SwiftUI

// S1 스파이크 앱 엔트리. 목적: MapLibre Native + buildStyle() JSON + 원격 PMTiles 렌더 검증.
// 본 이식(§10 M1~)에서 TabView 4탭 셸로 확장된다.
@main
struct HiHeightApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
